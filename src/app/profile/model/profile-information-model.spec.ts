import { ProfileInformationModel } from './profile-information-model';

describe('ProfileInformationModel', () => {
  it('should create an instance', () => {
    expect(new ProfileInformationModel()).toBeTruthy();
  });
});
